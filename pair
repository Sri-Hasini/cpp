/*              Pair
pair can store 2 values {first,second} that can consist of different datatypes also
pair is a simple container defined in <utility> header
we can also store nested pairs like
pair <int , pair<int,int>> p = {1, {2,3}};
*/
#include <bits/stdc++.h>
using namespace std;

int main() {
    pair <int,int> p={1,2};
    cout << p.first << " " << p.second << endl;//1 2
    pair <int, pair <int,int>> p1={1,{2,3}};
    cout << p1.first << " " << p1.second.second << " " << p1.second.first << endl;//1 3 2
    pair <int,int> a[]={{1,2},{3,4},{5,6}};
    cout << a[1].second << endl;//4
}
